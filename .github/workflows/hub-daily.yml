name: Hub Daily IMDB Update

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Run hub-imdb.js
        env:
          SUPABASE_URL: ${{ secrets.HUB_SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.HUB_SUPABASE_KEY }}
        run: node scripts/hub-imdb.js

      - name: Run hub-popular.js
        env:
          HUB_SUPABASE_URL: ${{ secrets.HUB_SUPABASE_URL }}
          HUB_SUPABASE_KEY: ${{ secrets.HUB_SUPABASE_KEY }}
          HUB_RAPIDAPI_KEY: ${{ secrets.HUB_RAPIDAPI_KEY }}
        run: node scripts/hub-popular.js
